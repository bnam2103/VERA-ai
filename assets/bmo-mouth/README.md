# BMO mouth (legacy)

The BMO screen now uses an **SVG mouth** in `index.html` (vector smile + filled mouth driven by Web Audio). The PNG files in this folder are **not** loaded by the app anymore; you can delete them or keep them for reference.
